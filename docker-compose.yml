services:
  umkm-queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: umkm-queue
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:-}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
      - ADMIN_SECRET=${ADMIN_SECRET:-qlite-secure-admin-session-secret-key-prod-2026}
      - MERCHANT_SECRET=${MERCHANT_SECRET:-qlite-secure-merchant-session-secret-key-prod-2026}
    labels:
      - com.centurylinklabs.watchtower.enable=true
      - com.centurylinklabs.watchtower.scope=gfea
    networks:
      - gfea-network

networks:
  gfea-network:
    external: true
